# Phase6 壁打ちキックオフ（TL → PdM）

**ステータス: 要件定義これから。具体的な方法・仕様は詰めなくてよい。まずは背景と大枠のアイデアを共有し、壁打ちの出発点にするための資料。**

## 0. このプロジェクトの前提

Twitter VTuberキャラクター「さく」のツイートを自動生成・投稿するシステム。`producer`（情報収集・メモ生成）と`consumer`（ツイート生成・投稿）を、PostgreSQL上の`memo_mq`テーブルで疎結合に繋ぐ構成。詳細な設計は[ARCHITECTURE.md](ARCHITECTURE.md)（Phase1時点の記述で古いので参考程度）、経緯全体は[PROGRESS.md](PROGRESS.md)を参照。

**現在地**: Phase5でペルソナ転換（「技術者」→「ゲーム開発をしている個人Vtuber」）を実施済み。fine-tuningは一旦見送り、素の`gpt-5.5`+プロンプトで運用中。新ペルソナでの実投稿の最終確認・Railwayデプロイともに完了済み（Railway上のcron自動実行で新ペルソナでの実投稿を確認済み）。

**プロダクトオーナーからの追加情報**: ゲーム開発自体は本プロジェクトと同時進行で進めている。案2（開発中ゲームの紹介ツイート）は「ゲームが未完成だから時期尚早」ではなく、**今すぐ詳細設計に進めてよい**とのこと（データ構造などは素材が揃う前に先に決めておける）。§4「まだ何も決まっていないこと」のうち、この時間軸の懸念は解消済み。

## 1. 現状の構成（input側）

`data-collector`が3種類のproducerから情報を集め、AIで「さく」目線のメモに変換して`memo_mq`に書き込む。1日1回のデイリーバッチ。

| producer | 何をするか |
|---|---|
| YouTube | 指定プレイリストの直近動画からネタを拾う |
| Qiita | ゲーム開発タグの技術記事からネタを拾う |
| Agent SDK | Claude Agent SDKにWeb検索させ、ゲーム開発関連の話題を自律調査してネタを2件拾う |

3つとも「外部の情報源を読んで、さく目線のメモを1つ作る」という共通の型で、`memo_mq`という1つの出口に集約している。

## 2. 現状の構成（output側）

`twitter-VCharacter`が`memo_mq`から未使用メモを1件取り出し、AIで「本文」「タグ」を生成して結合し、Xに投稿する。**現状は1種類のみ**。

```
memo_mq → 本文生成(AI) → タグ生成(AI) → 結合 → X投稿
```

## 3. Phase6でやりたいこと（大枠のアイデア、TLからの提案）

inputが3種類あるのと同じように、**outputも3種類に増やしたい**、というのが今回の出発点。

1. **既存の延長**: `memo_mq`を使った簡単な文章投稿（今の仕組みそのまま）
2. **開発中ゲームの紹介ツイート**: `memo_mq`とは別のDB（まだ存在しない）に置く「開発中ゲームの情報（紹介動画・画像等）」をもとにツイートを作る
3. **Vtuberとしての親しみやすいツイート**: `memo_mq`（＝外部情報のメモ）に依存しない、キャラクターとしての素の発信。例えば「Agentに毎回話題になりそうなネタを探させる」ような形で、雑談・日常的な内容を作れないか

**狙い**: 現状の投稿は「外部情報を素材にした気づき・学びツイート」に偏っている。inputを増やしても、outputの型が1つしかないと結局似たようなツイートしか生まれない。outputの型自体を増やすことで、投稿のバリエーション・人間らしさを広げたい。

## 4. まだ何も決まっていないこと（PdMに壁打ちしてほしい範囲）

- 3つのoutputをどう使い分けるか（曜日で切り替える？ランダム？別々のcronで動かす？）
- 「開発中ゲームの紹介」用のDB・データ構造は何もない。何を保存するか（動画URL、画像、テキスト説明？）はゼロから設計が必要
- 「Vtuberとして親しみやすいツイート」の具体像がまだ曖昧。「Agentに毎回ネタを探させる」はTLの思いつきの一例で、他の実現方法（固定の話題リストからランダム選択、天気や時事ネタ、等）もあり得る
- 3つのoutputが「同じ`TextPublisher`（X投稿）を共有しつつ中身の生成ロジックだけ分岐する」のか、それとも「投稿の頻度・タイミングごと分ける」のか、アーキテクチャ上の設計方針も未定
- 既存の`memo_mq`パターン（producer/consumerをDBで疎結合にする設計）を新しいoutputにも踏襲すべきか、それとも別の設計にするか

## 5. 今回PdMに期待すること

- 具体的な仕様・実装方針を決め切る必要はない
- まず「3つのoutputを増やす」というTLのアイデアについて、目的・優先順位・懸念点を一緒に整理してほしい
- 特に2番目（開発中ゲームの紹介）は「開発中ゲーム」自体の実体がまだない（Phase5のSNSポジショニング転換の前提が、ゲームの存在を仮定している）ため、この施策がどのタイミングで現実的になるか、時間軸の整理もしてほしい
- 壁打ちの結果、方向性が固まったら、通常のPhase運用と同じく`PROGRESS.md`に記録し、詳細要件が固まった段階で実装指令書（`HANDOFF_PHASE6.md`）を別途作成する

## 6. 参考: 関連する既存ドキュメント

- [SNS_POSITIONING_PROPOSAL.md](SNS_POSITIONING_PROPOSAL.md) — 「ゲーム開発する個人Vtuber」というポジショニング転換の背景・段階的な進め方（Phase A〜C）。今回のoutput多様化はこの延長線上にある
- [PROGRESS.md](PROGRESS.md)の「Phase5」セクション — ペルソナ転換の詳細、fine-tuning見送りの判断根拠
- `twitter-VCharacter/src/config.rs` — 現状のconsumer側のDI構成（`AiGenerator` + `ImageGenerator`, `TextPublisher` + `MediaUploader`, `MemoQueue`の3つ）。画像生成の仕組み自体はPhase4で実装済み・Phase5で一旦コメントアウトして停止中（[main.rs](twitter-VCharacter/src/main.rs)参照）なので、2番目のoutput案で再利用できる可能性がある
