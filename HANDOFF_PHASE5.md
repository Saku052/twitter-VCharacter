# Phase5 実装指令書（TL → AIエンジニア）

**このファイルだけ読めば着手できるように書いてある。前提知識ゼロで読んでよい。**

方針が明確な変更は確認を挟まず進めてよい（`CLAUDE.md`の「Claude Code としての振る舞い方」を参照）。判断に迷う場合のみユーザーに確認すること。詳細な決定の経緯は`PROGRESS.md`・`SNS_POSITIONING_PROPOSAL.md`に記録されている。矛盾を感じたらそちらを一次情報として参照すること。

## 0. このタスクの性質

これは新しいデータソース追加や新機能実装ではない。**「さく」のペルソナ自体を転換する**タスクで、対象はプロンプト文面とfine-tuningモデルの2つ。コード構造の変更はほぼ発生しない想定。

## 1. 背景・経緯

現状の「さく」は「技術が好きな社会人1年目エンジニア」として発信している。プロダクトオーナーから「技術系クラスタは発信者が飽和していて、絡みにいく相手が曖昧」という課題認識が出た。TLがリサーチした結果（詳細は[SNS_POSITIONING_PROPOSAL.md](SNS_POSITIONING_PROPOSAL.md)参照）、以下の方針が確定した。

**確定方針**: 「ゲーム制作が好きな社会人1年目エンジニア」の主従を入れ替え、**「ゲーム開発をしている個人Vtuber」を主軸**にする。理由は2つ。
1. 技術クラスタはレッドオーシャン。個人Vtuber×ゲーム開発の掛け合わせはニッチで差別化できる
2. ポジションを変えることで、将来「他の個人Vtuberに自作ゲームを遊んでもらう」という営業活動の導線が生まれる（技術者のままではこの相手に接続する動線がない）

**注意**: 完全にエンジニア要素を捨てるわけではない。「ゲーム開発をしている」という開発者性は残しつつ、発信の軸足を「エンジニアとしての学び」から「個人Vtuber・ゲーム開発者としての日常・進捗・気づき」に移す、というニュアンス。

## 2. 対象範囲（今回のスコープ）

以下の3点。**コード変更は伴わない**想定（`main.rs`のプロンプト定数の文面変更のみ）。

1. `twitter-VCharacter/src/main.rs`の`BODY_SYS_PRPT`・`TAG_SYS_PRPT`のロール記述変更
2. `data-collector/src/main.rs`の`SYSTEM`（メモ生成プロンプト）のロール記述変更
3. `GPT_MODEL`（fine-tunedモデル）の再学習、または新規fine-tuning

data-collector側のQiitaクエリ（ゲーム開発タグへの絞り込み）は**このタスクのスコープ外**。別タスクとして既に方針が決まっている（`tag:ゲーム開発`単体、`stocks`/`created`条件は実測して調整、詳細は§6参照）ので、ここでは触れない。

## 3. 現在の実装（変更前）

### 3.1 `twitter-VCharacter/src/main.rs`

```rust
const GPT_MODEL: &str = "ft:gpt-4.1-2025-04-14:personal:tweetsource1:DfS5fKl8";
const BODY_SYS_PRPT: &str = "<role>技術が好きな社会人1年目のエンジニア</role>
<task>渡されたメモを元に、本人視点のツイート本文を生成する</task>
<rules>
- 140字以内
- ハッシュタグは含めない（別途生成するため）
- 砕けた口語（「〜なんだよな」「〜じゃん」「〜かもしれない」など）と断定調を内容に応じて使い分け
- 絵文字は0〜2個、内容に応じて自然に配置
- 自慢や説教にならず、気づきや失敗を等身大で書く
</rules>";

const TAG_SYS_PRPT: &str = "<role>技術が好きな社会人1年目のエンジニア</role>
<task>渡されたメモを元に、ツイートに付けるハッシュタグを考える</task>
<rules>
- 内容に関連するタグを1〜2個
- 「#」は付けず、カンマ区切りで出力する（例: Rust,個人開発）
- 説明文や前置きは付けず、タグの文字列のみを出力する
</rules>";
```

`GPT_MODEL`は`gpt-4.1-2025-04-14`をベースに、過去のツイート例で**fine-tuning済み**のモデル。「技術者としての語り口」がこの学習データに焼き付いている可能性が高い（§5参照）。

画像生成プロンプト（`IMAGE_PROMPT_TEMPLATE`）も「技術系VTuberのツイート添付を想定した」という記述があり、ここもペルソナ転換の影響を受ける（§4.3参照）。

### 3.2 `data-collector/src/main.rs`

```rust
const SYSTEM: &str = "<role>さく担当の編集者。ネタ元を読んで、さくが話したくなりそうなポイントだけ抜き出す</role>
<task>渡された情報（[YouTube動画]または[Qiita記事]）を読み、ネタにできそうな『気づき』『学び』『感想の種』を日本語の短いメモとして1個出力する</task>
<rules>
- ハッシュタグや絵文字は付けない
- 事実をそのまま要約するのではなく『これ面白いな』『これ自分でも試したい』のような感想・気づきの形に変換する
- 専門用語は無理に避けず、ただし社会人1年目が背伸びしすぎない温度感で
- コードや型名など込み入った専門用語は、メモの時点で噛み砕く（ツイート生成側では元情報を参照できないため）
- 50文字以内
</rules>";
```

このプロンプトは`YouTube`/`Qiita`/`Agent SDK`の3つのproducer共通で使われる。`<role>`に「エンジニア」という明言はないが、「専門用語は無理に避けず」「コードや型名など込み入った専門用語」というrulesが技術記事前提の書き方になっている。

## 4. 決定事項・進め方

### 4.1 段階的に進める（プロンプト修正 → 検証 → fine-tuning）

いきなりfine-tuningから着手しない。理由: fine-tuningはコスト・時間がかかる上、元に戻すのが難しい。まずプロンプト修正だけでどこまで変わるかを見てから、fine-tuningの必要性・データセット設計を判断する。

**Step 1**: `BODY_SYS_PRPT`・`TAG_SYS_PRPT`・`SYSTEM`（data-collector）の`<role>`を書き換える（§4.2）
**Step 2**: `GPT_MODEL`は変更せず（fine-tuned modelのまま）、プロンプト変更のみで`cargo run`を数回実行し、出力を目視確認する（§5）
**Step 3**: Step2の結果、「技術者らしい語り口」が残り続けるようなら、fine-tuning着手に進む（§5.3）

### 4.2 プロンプトの書き換え方針

`<role>`の記述を「技術が好きな社会人1年目のエンジニア」から、以下のような形に変更する（文面はAIエンジニアの裁量で調整してよいが、以下の要素は含めること）。

- 「ゲーム制作をしている個人Vtuber」を主軸にする
- エンジニアである設定は完全には消さない（「社会人1年目」という背景は活かしてよい）が、語りの中心を「技術的な学び」から「ゲーム開発の進捗・気づき・個人Vtuberとしての発信」に移す

`<rules>`は現状維持でよい（口調・絵文字・文字数などのルールはペルソナ転換と直接関係しない）。ただし`BODY_SYS_PRPT`の「自慢や説教にならず、気づきや失敗を等身大で書く」は転換後も重要な軸なので削らないこと。

data-collector側の`SYSTEM`は、「専門用語は無理に避けず」「コードや型名など込み入った専門用語」のくだりが技術記事前提になっているため、ゲーム開発・個人Vtuber文脈のネタ（Qiitaのゲーム開発タグ記事、YouTubeのゲーム開発系プレイリスト、Web検索）にも自然に対応できる表現に調整すること。

### 4.3 画像生成プロンプトも対象に含める

`IMAGE_PROMPT_TEMPLATE`の「技術系VTuberのツイート添付を想定した」という記述も、ペルソナ転換に合わせて「ゲーム開発系Vtuber」に変更すること。スコープ漏れしやすい箇所なので明記する。

## 5. fine-tuningについて

### 5.1 なぜ検討が必要か

`GPT_MODEL`（`ft:gpt-4.1-2025-04-14:personal:tweetsource1:DfS5fKl8`）は、過去のツイート実例で学習されたfine-tunedモデル。プロンプトの`<role>`を書き換えても、fine-tuningで学習した「技術者らしい語彙・視点・話題の選び方」の傾向が出力に残り続ける可能性がある（Phase3.5でタグVec化した際も「fine-tunedモデルがプロンプト変更に安定して追従するか未検証」という懸念が実際に出た前例がある）。

### 5.2 判定基準

Step2（§4.1）で`cargo run`を最低5回程度実行し、生成された本文を確認する。以下のいずれかに該当すれば、プロンプト修正だけでは不十分と判断し、fine-tuning着手に進む。

- 生成文に技術用語・技術者特有の言い回し（「実装してみた」「バグを直した」等）が頻出し、ゲーム開発・Vtuberらしい語り口になっていない
- ロール変更を無視したかのような、変更前とほとんど変わらない出力が続く

### 5.3 fine-tuning着手時の進め方（該当した場合）

1. **新しい学習データセットの用意**: 「ゲーム開発をしている個人Vtuber」らしいツイート文の実例を用意する必要がある。ゼロから書き起こすか、既存の学習データ（元の`tweetsource1`データセット）をベースに文体だけ書き換えて再利用するか、AIエンジニアの判断で進めてよい。どの道を取ったか・件数はPROGRESS.mdに記録すること
2. **既存モデルの扱い**: 新規にfine-tuningするか、既存モデルへの追加学習（差分学習）にするかは、OpenAIのfine-tuning APIの仕様に従って判断してよい。コスト・所要時間の見積もりを一度PROGRESS.mdに記録してから本番実行すること
3. **`GPT_MODEL`定数の更新**: 新しいfine-tunedモデルIDができたら、`twitter-VCharacter/src/main.rs`の`GPT_MODEL`を更新する
4. **旧モデルの保持**: 旧モデルID（`ft:gpt-4.1-2025-04-14:personal:tweetsource1:DfS5fKl8`）はPROGRESS.mdに記録しておくこと。新モデルの出力が不安定だった場合に切り戻せるようにするため

## 6. スコープ外（今回やらないこと）

- data-collector側のQiitaクエリ変更（`tag:ゲーム開発`への絞り込み）。既に方針決定済み（実測: `stocks:>1`、直近30日で28件）だが、これは別タスクとして扱う。混同しないこと
- YouTubeプレイリストの中身の入れ替え（ゲーム開発・Vtuber系動画への差し替え）は、プロダクトオーナー自身がプレイリストを編集する想定。コード変更は不要
- Agent SDK（`agent-wrapper/main.py`）の`TASK_PROMPT`変更（「今日の技術トレンドを1つ調べて」の文面調整）。これも別途扱う
- リプライ・エンゲージメント機能の実装（`SNS_POSITIONING_PROPOSAL.md`のPhase B以降）
- 140字超過チェック、タグ0件時の空行など、Phase3.5から継続する既知の残課題（このタスクとは無関係）

## 7. 受け入れ基準

1. `twitter-VCharacter`・`data-collector`双方で`cargo check`が通る（プロンプト定数の文字列変更のみなのでコンパイルには影響しないはずだが、念のため確認する）
2. Step2（プロンプト修正のみ）の状態で`cargo run`を最低5回実行し、生成された本文・タグを目視確認した結果をPROGRESS.mdに記録する
3. §5.2の判定基準に基づき、fine-tuning着手の要否を判断し、その判断根拠をPROGRESS.mdに記録する
4. fine-tuningに着手した場合、新モデルでの生成結果を最低5回分確認し、旧モデルと比較した所感をPROGRESS.mdに記録する
5. 画像生成プロンプト（`IMAGE_PROMPT_TEMPLATE`）の変更漏れがないこと

## 8. 実装完了後にやること

1. このファイル（`HANDOFF_PHASE5.md`）は完了後に削除してよい
2. `PROGRESS.md`に「Phase5」セクションを新設し、決定事項・変更したプロンプト文面・fine-tuningの判断根拠と結果・旧モデルIDを記録する
3. `SNS_POSITIONING_PROPOSAL.md`のPhase A該当部分が完了した旨を追記する
