# 投稿反響分析 方針

**ステータス: 方針案（実装・実行はまだ）。** 対象読者: プロダクトオーナー、TL。取得バッチの実装はこの方針が固まってから別途行う。

---

## 0. 結論（先に要点）

1. **既存の151件で「プロンプトをどう変えるか」は決められない。** 検出できるのは1.5〜2倍規模の差だけ。プロンプト調整で現実的に期待できる1〜3割の差は、原理的に見えない（§3）
2. **しかも151件のうち、同じ条件で投稿されたものは約90件しかない。** ペルソナ転換・19日間の停止・滞留メモの消化で、期間ごとに条件が違う（§1）
3. **既存データの役割は3つに絞る**: ①測定基盤の検証、②大きな差（時間帯など）があれば拾う、③仮説リストを作る
4. **プロンプト変更の判断は、今後の「ランダム割付の比較」で行う。** 1投稿ごとにコインを投げて旧プロンプトと新プロンプトを使い分ける。自動最適化ではない。人間が仮説を1つ選び、結果を読んで決める。1仮説あたり6週間程度かかる（§6）
5. **既定の判断は「何もしない」。** 事前に決めた条件をすべて満たしたときだけ変える（§6）
6. 伸びたツイートを例示に使う案は**非推奨**。懸念の3点に加えて「平均への回帰」の問題がある（§7）

---

## 1. 前提: データの中身は均質ではない

コードとPROGRESS.mdの運用履歴から、151件（2026-07-19〜09-24）は次の期間に分かれる。**集計の前に必ず期間フラグを付ける。**

| 期間 | 件数（概算） | 条件 | 扱い |
|---|---|---|---|
| **P0** 07-19〜07-20 | 数件 | 旧ペルソナ（技術者）、fine-tunedモデル。Phase5の動作確認で旧ペルソナのメモが1件投稿されている | **除外** |
| **P1** 07-20〜08-05 | 約50 | 新ペルソナ、gpt-5.5。**Agentメモの破損対策（Phase C1）前** | 採用。ただし破損メモ由来の投稿にはフラグを付ける |
| 停止 08-05〜08-25 | 0 | X APIクレジット枯渇 | — |
| **P2** 08-25〜09-24 | 約90 | 破損対策後。滞留メモを消化中のため、**メモ作成から投稿まで2週間以上ずれた古い話題が多い** | 採用（主な分析対象） |

件数は概算なので、取得後に実数で確認する。

分析設計に効く事実がほかに3つある。

- **P1・P2の間、プロンプト（`BODY_SYS_PRPT` / `TAG_SYS_PRPT`）は一度も変わっていない。** したがって既存データからは「どのプロンプトが良いか」はわからない。わかるのは「同じプロンプトの下で、どんなメモ・本文の特徴が反響と相関したか」だけ
- **タグは全件に付いている**（タグ生成を毎回呼んでいる）。「タグあり/なし」の比較は変動がないので成立しない。例外はパース失敗でタグ0件になった投稿だけ
- **投稿時間帯とsourceは独立していない可能性がある。** `fetch_latest_memo`は`ORDER BY created_at`で、`created_at`は日付型、同日内の順序指定がない。実際には挿入順（youtube→qiita→agent）で取られやすく、「朝の枠はqiitaが多い」といった偏りが生まれうる。§10のクエリ①でクロス集計して確認する

---

## 2. 指標設計

### 主指標: エンゲージメント率（ER）

```
ER = (like + retweet + reply + quote + bookmark) / impression
```

- **いいね率ではなく合算を使う理由**: 分子の大半はいいねになるはず。それでも合算したほうがイベント数が増え、ノイズが減る。重みを付けない単純合算にするのは、恣意性をなくすため
- **重み付き複合指標（例: リプライ×5）を採らない理由**: 重みの決め方に根拠がない。nが小さいと「重みを変えたら差が出た」という後付けの自由度になる
- **集計方法の注意**: 1件ごとのER（例: 3/40 = 7.5%）の平均は使わない。インプレッションが数十件の投稿ではノイズが大きすぎる。群単位では **Σエンゲージメント / Σインプレッション** のプール率を出す。信頼区間は投稿単位のブートストラップで求める。回帰モデルを使うなら、負の二項回帰で`log(impression)`をオフセットにする
- **測定時点を揃える**: インプレッションは投稿後もしばらく増え続ける。**投稿から7日以上経ったものだけを分析対象にする**（今後の投稿はT+7dのスナップショットを取るのが理想。§8）

### 副指標: インプレッション（到達）

ERは「見た人に刺さったか」、インプレッションは「どれだけ配られたか」を表す。**時間帯とタグは主に到達に効き、本文の中身は主にERに効く**と考えられる。比較軸ごとに見る指標を分ける（§4）。

### 戦略上の最重要指標: 外部からのリプライ・引用（統計には使わない）

SNS_POSITIONING_PROPOSAL.mdの方針は「個人Vtuber界隈との関係づくり」。これに照らすと、本当に重要なのは**誰から反応が来たか**になる。ただしリプライはほぼ0件に集中すると予想され、統計的な比較には向かない。

- **全件を目視で確認する。** 月に数件程度のはずなので確認は現実的
- 記録するのは「身内・bot以外の**異なるアカウント数**」の月次推移だけ。北極星指標として追うが、差の検定には使わない

### ガードレール（悪化していないことを確認する指標）

- フォロワーの純増減（現在は未取得。§8）
- ペルソナの逸脱や「AI臭さ」（月次レポートで上位・下位5件を人間が読んで確認）

### 除外・補正すべきノイズ

- **身内のエンゲージメント**: プロダクトオーナーやTLの個人アカウントからのいいね等。あるなら補正が必要。まず実態を確認する
- **スパム・botからのリプライ**

---

## 3. 検出力の見立て: n=151で何が見えるか

2群比較（有意水準5%・両側、検出力80%）で必要な1群あたりの件数は、効果量dに対して概ね **n ≈ 16 / d²** になる。

| 効果量 d | 目安 | 1群あたりの必要件数 | 1日3投稿を2群に分けた場合の所要期間 |
|---|---|---|---|
| 0.8 | ERが約2倍 | 25 | 約2.5週間 |
| 0.5 | ERが約1.5倍 | 64 | 約6週間 |
| 0.3 | ERが約1.3倍 | 175 | 約4ヶ月 |
| 0.2 | ERが約1.2倍 | 390 | 約9ヶ月 |

※「ERが○倍」は投稿ごとのERの変動係数を1前後と仮定した換算。実データで変動係数を測ってからこの表を更新する。

この表を現状のデータに当てはめると、次のようになる。

- **時間帯（3枠×約50件）**: d≈0.55以上なら検出できる。大きな差がありうる軸なので、見る価値はある
- **agent（106件）vs qiita（36件）**: d≈0.55以上。ただし期間や話題の交絡が強い（§5）
- **youtube（9件）**: d≈1.0以上でないと検出できない。**比較不能として扱う**
- **タグ別・本文特徴別**: 1カテゴリあたりの件数が小さくなるうえ、比較の数が多いので偶然の差が出やすい。**記述統計止まり**
- **多重比較の注意**: 切り口を10個見れば、差が実在しなくても1個以上が「有意」になる確率は約40%。見る比較は**事前に決めた少数に限る**
- **月次で繰り返し見る注意**: 同じ仮説を毎月検定し直すと、偽陽性がさらに増える。判定するタイミングは事前に固定する

**見立て: 既存データで何らかの差を検出できるのは、差が1.5倍以上ある場合だけ。** プロンプトの微調整で起きる程度の差は、観察データを何ヶ月貯めても検出がほぼ無理。ランダム割付で1仮説ずつ検証するほうが早い。

---

## 4. 分析の手順（ゲート付き）

各ステップの結果次第で、先に進むかどうかを決める。

### Step 0. 取得と突き合わせ
- X APIで全投稿を取得し、`memo_mq` / `posted_tweets`と突き合わせる。**X側の`created_at`と`used_at`の時刻差（±10分）を主キーにし、本文一致を補助に使う**
- 突き合わせられなかった投稿は、手動投稿やテスト投稿の可能性があるので除外する
- `used_at`はtext型なので、タイムゾーンと書式を確認する。時間帯の判定はX側の`created_at`（UTC→JST）を正とする

### Step 1. 品質フラグを付ける
- 期間（P0/P1/P2）、破損メモ由来、旧ペルソナ、画像付き（Phase4〜5の短期間に存在しうる）、タグ0件
- メモの鮮度として**`投稿日 − memo.created_at`（日数）**を計算しておく

### Step 2. 全体像の把握 → **ゲートA**
- インプレッションとERの分布、週ごとの推移
- **ゲートA: 投稿ごとの差は、偶然のばらつきを超えているか？**
  - 各投稿のエンゲージメント数が「全体のER × その投稿のインプレッション」を平均とするポアソン分布からどれだけ外れるか（過分散）を見る
  - **過分散がほぼない場合**、投稿ごとの差はすべて偶然のノイズであり、本文の中身で説明できる差は存在しない。**Step 5（本文特徴）は中止**し、「現状の規模では中身の改善より到達（関係づくり）がボトルネック」と結論する。十分ありうる結果なので、そう出たら受け入れる
  - インプレッションの中央値が数十件以下なら、同じ結論になりやすい

### Step 3. 時間帯（最もきれいに比較できる軸）
- 毎日3枠すべてに投稿しているので、**同じ日の中で枠同士を比べられる**。日ごとの効果（フォロワー数の増加、その日の話題）が相殺されるため、交絡に最も強い
- 指標はインプレッションを主に見て、ERを従に見る。検定は日単位の並べ替え検定、またはday固定効果付きの回帰
- 行動につながる変更先は、プロンプトではなくcron時刻

### Step 4. source（agent vs qiita）
- 期間・鮮度・時間帯を共変量に入れて比べる。youtubeは記述統計だけ
- 行動につながる変更先は、プロンプトではなくdata-collectorの収集配分

### Step 5. 本文の特徴（ゲートAを通過した場合のみ）
- **見る特徴は分析前に3つまでに固定する。** 候補:
  1. 文末が問いかけ・二択になっているか（SNS担当の提案で保留中の施策に直結する）
  2. 話題の種類（制作の進捗・詰まり / 日常 / 技術やニュースへの感想）。SNS_POSITIONING_PROPOSAL.md §3.1の比率設計と対応する
  3. 本文の長さ
- ラベル付けはLLMで行う。**判定基準（コードブック）は先に文書化し、20件程度は人間の判定と一致するか確認する**
- ここで出た差は採用せず、**次のランダム割付実験の候補に回すだけ**

### Step 6. 質的レビュー
- ER上位・下位それぞれ5〜10件を人間が読み、仮説の言葉にする
- 上位だけでなく**下位も必ず読む**。上位だけ読むと、成功例の共通点を探す確証バイアスが強く出る

### Step 7. 判断
- §6の基準に照らして判定する。出力は「変更する / 何もしない / 次に検証する仮説」の3区分

---

## 5. 交絡と、素朴な集計で誤読しやすい点

| 交絡 | 何が起きるか | 対処 |
|---|---|---|
| **期間（体制変更）** | P1とP2で条件が違う。停止明けは到達が落ちている可能性もある | 期間を共変量にする。**結論はP1とP2の両方で同じ向きになるかを確認** |
| **フォロワー数の推移** | 後の投稿ほどインプレッションが増える | 時間帯は日内比較で対処。そのほかは経過日数を共変量にする。今後はフォロワー数を日次で取る（§8） |
| **時間帯×source** | FIFOの取り出し順の都合で、枠ごとにsourceが偏りうる（§1） | クロス集計で確認し、偏りがあれば相互に共変量として入れる |
| **メモの鮮度** | P2はメモ作成から投稿まで2週間以上あき、ニュース系のagentメモほど古びている可能性がある | 鮮度（日数）を共変量にする |
| **source→話題→タグの連鎖** | タグはメモから生成されるので、「このタグが効いた」はsourceや話題の効果かもしれない | タグは記述統計にとどめ、因果的に解釈しない |
| **外れ値** | 誰かに1回RTされただけで、その群の平均が跳ねる | 中央値や順位ベースの手法も併用する。**上位1件を除いても結論が変わらないか**を必ず確認 |
| **測定時点** | 新しい投稿ほどインプレッションが伸びきっていない | 投稿から7日以上経ったものだけを使う |
| **身内のいいね** | 身内がいいねしやすい話題に引っ張られる | 実態を確認し、必要なら除外する |
| **平均への回帰** | 伸びた投稿の「共通点」の多くは偶然 | §7参照 |

---

## 6. 判断基準（既定は「何もしない」）

### 6.1 既存データ（観察データ）からの判断

- **観察データだけを根拠にプロンプトを変えることはしない。** 出力するのは仮説リストだけ
- 例外は**品質問題**。破損メモ由来の投稿、ペルソナ逸脱、AIの独り言の混入などは統計と無関係なので、見つけ次第直す
- 例外その2は**時間帯**。Step 3で後述6.2の条件1・3・4を満たす差が出たら、cron時刻の変更を検討してよい。変更のコストが低く、元に戻すのも容易なため

### 6.2 ランダム割付実験での判断（推奨する本線）

**実験の設計**
- 1投稿ごとに、現行プロンプト（A）と変更案（B）をランダムに割り付ける。**1日の中でAとBが偏らないブロック割付**にし、時間帯の交絡を消す
- 割り付けたプロンプトの版を`posted_tweets`に記録する
- **同時に検証する仮説は1つ**。期間と判定日は開始前に決める（例: 6週間・各群約64件。検出できるのはd≈0.5以上）
- 途中で結果を見て止めない

**Bを採用する条件（すべて満たすこと）**
1. 事前に決めた主指標（ER）で、B/Aの比の95%信頼区間の下限が1.0を超える
2. 比の点推定が**1.2以上**。統計的に有意でも、差が小さければ変更のコスト（ペルソナの揺らぎ）に見合わない
3. 実験期間の前半と後半で、差の向きが一致する
4. 各群の上位1件を除いても条件1が崩れない
5. ガードレール（フォロワーの純減、ペルソナの逸脱）が悪化していない

**どれか1つでも満たさなければAを維持する。** 記録は「差がなかった」ではなく、「**○倍以上の差はなかった**（信頼区間の上限）」とする。こう書けば、次に同じ仮説を試すかどうか判断できる。

なお、データで差がつかなくても、ペルソナへの合い方など**質的な理由でBを選ぶのは正当な判断**。その場合は「データに基づく判断ではない」と明記して選ぶ。

### 6.3 月次レポートでの警報基準

- 月ごとのプールERが、**過去3ヶ月の月次ERの範囲を下回ったとき**だけ原因を調査する。それ以外の月ごとの上下は反応しない
- 投稿件数が想定（約90件/月）から大きく欠けていないか。これは停止の検知を兼ねる

---

## 7. 伸びたツイートをプロンプトに例示することについて

**非推奨。** 挙げられている3つの懸念（相関と因果の混同、少数例への過適合、出力の均質化）はどれも妥当。さらに次の問題がある。

- **平均への回帰（勝者の呪い）**: n=151で上位に来た投稿は、中身の良さより偶然のブレで選ばれた割合が大きい。上位5件を例示しても、次の投稿は平均的な位置に戻る可能性が高い
- **LLMは表面を真似る**: 例示から写し取られるのは、「伸びた理由」という本質より、文の構造・言い回し・絵文字の位置といった表面になりやすい。フォロワーから見て「またこの型か」になり、ペルソナの自然さを損なう
- **効果を検証できなくなる**: 例示は多くの特徴を同時に変える。何が効いたのか後から切り分けられない

**代わりにやること**
- 上位・下位のレビューから**抽象的な仮説**を1つ取り出し（例:「文末に具体的な問いかけを置く」）、`<rules>`に1行足した版をBとしてランダム割付で検証する
- 例示を使うなら、選ぶ基準は反響ではなく**ペルソナへの合い方**にする（人間が選ぶ）。話題の違うものを5件以上、ローテーションで使い、「内容は真似しない」と明記する
- 例示より安全で効果が大きいのは**悪い例（避けるべき出力）**の提示。AIの独り言、説教調、広告っぽさなど。これは統計に依存せず品質を上げられる

---

## 8. 追加で取るべきデータ

優先度順。

| 優先度 | データ | 理由 | 取り方の案 |
|---|---|---|---|
| **高** | **プロンプトの版** | 今後の体制変更やA/B割付を区別するのに必須。今回の分析で一番苦労する点が「いつ何が変わったか」の復元 | `posted_tweets`に`prompt_version`（とモデル名）の列を足す |
| **高** | **T+7dの指標スナップショット** | 測定時点を揃えるため。さらに`user_profile_clicks`などの非公開指標は30日で取れなくなる | 日次バッチで、投稿7日後の投稿の指標を保存する |
| **高** | **フォロワー数の日次推移** | 交絡の補正とガードレールに使う。過去の分は取れないので、早く始めるほど良い | `GET /2/users/me`のpublic_metricsを日次で保存する |
| 中 | 反応したアカウント（リプライ・引用の主） | 北極星指標（外部の異なるアカウント数）と、身内・botの除外に使う | 月次でリプライ・引用を取得する。いいねした人までは不要 |
| 中 | 人間側の活動ログ | 中の人が手動でリプライや交流をした日は到達が変わる | 簡単なメモで十分 |
| 低 | 本文特徴のラベル | Step 5用 | 分析時にLLMで付与。保存は不要 |

※ 既存投稿の`user_profile_clicks`などの非公開指標は、直近30日（P2の大半）の分しか取れない。**取得バッチを実装するなら、これを最初に取る**（日が経つほど取れる件数が減る）。

---

## 9. 現時点のデータで答えられること / 答えられないこと

**答えられる**
- 反響の水準と分布、週ごとの推移（そもそも比べる意味があるほど反響があるか）
- 投稿ごとの差が偶然を超えているか（ゲートA）
- 時間帯による大きな差（1.5倍以上）
- agentとqiitaの大きな差（共変量で補正したうえで）
- 品質問題のある投稿の洗い出し
- 次に検証する仮説のリスト

**答えられない**
- **どのプロンプトが良いか**（プロンプトを変えていないので比較対象がない）
- youtube由来の投稿の良し悪し（9件）
- 個別タグの効果
- 本文スタイルの細かい効果（1〜3割規模の差）
- どの要因でも、因果としての効果（観察データのため）
- フォロワー獲得への寄与（フォロワー数の推移がない）
- P1（〜08-05）の非公開指標（30日を超えている）

---

## 10. 探索用クエリ案（DBのみで今すぐ確認できるもの）

X APIを叩く前に、交絡の有無をDBだけで確認できる。`used_at`はtext型なので、書式とタイムゾーンを確かめてから使う。

```sql
-- ① 時間帯×sourceのクロス集計（FIFOによる偏りの確認）
SELECT
  CASE
    WHEN EXTRACT(HOUR FROM (used_at::timestamptz AT TIME ZONE 'Asia/Tokyo')) < 12 THEN '08:15'
    WHEN EXTRACT(HOUR FROM (used_at::timestamptz AT TIME ZONE 'Asia/Tokyo')) < 20 THEN '17:55'
    ELSE '21:45'
  END AS slot,
  source,
  COUNT(*)
FROM memo_mq
WHERE used_at IS NOT NULL
GROUP BY 1, 2
ORDER BY 1, 2;

-- ② 期間ごとの件数とsource内訳
SELECT
  CASE
    WHEN used_at::timestamptz < '2026-07-21' THEN 'P0'
    WHEN used_at::timestamptz < '2026-08-20' THEN 'P1'
    ELSE 'P2'
  END AS period,
  source,
  COUNT(*)
FROM memo_mq
WHERE used_at IS NOT NULL
GROUP BY 1, 2
ORDER BY 1, 2;

-- ③ メモの鮮度（作成から投稿までの日数）をsource・期間別に
SELECT
  source,
  used_at::timestamptz >= '2026-08-20' AS is_p2,
  percentile_cont(0.5) WITHIN GROUP (ORDER BY (used_at::timestamptz::date - created_at)) AS median_lag_days,
  MAX(used_at::timestamptz::date - created_at) AS max_lag_days
FROM memo_mq
WHERE used_at IS NOT NULL
GROUP BY 1, 2;

-- ④ 破損メモ由来の投稿候補（Phase C1の除外パターンの一部）
SELECT id, source, used_at, memo
FROM memo_mq
WHERE used_at IS NOT NULL
  AND (memo ~ '(以下、|メモ[0-9１-９]|\*\*|CLAUDE\.md|https?://)' OR length(memo) > 80);
```

P0の境界（07-20のどの投稿からが新ペルソナか）は日付だけでは切れない。本文を目視して確定させる。

---

## 11. 月次運用の型（案）

月1回、人間が読むレポートの構成。

1. **稼働状況**: 投稿件数、失敗・欠落、フォロワーの純増減
2. **主指標**: 月次のプールERとインプレッション中央値。過去月と並べ、95%信頼区間を付ける
3. **北極星指標**: 外部からリプライ・引用をくれた異なるアカウント数と、その中身
4. **実行中の実験**: 仮説、開始日、判定日、現在の件数。**判定日までは結果を見ない**
5. **上位・下位5件の本文**（質的レビュー用）
6. **判断**: 変更する / 何もしない / 次の仮説。理由を1〜2行で書く
